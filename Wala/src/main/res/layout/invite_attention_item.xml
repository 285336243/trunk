<?xml version="1.0" encoding="utf-8"?>

<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent"
    android:background="@color/white" android:paddingBottom="16dp">

    <FrameLayout
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:id="@+id/invite_attention_avatar_layout"
        android:layout_marginLeft="12dp"
        android:layout_marginTop="16dp" >

        <ImageView
            android:id="@+id/invite_attention_avatar"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:scaleType="fitXY"
            android:background="@drawable/headpic"/>

        <ImageView
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="@drawable/feb_topicuserhead_tomato" />
    </FrameLayout>

    <LinearLayout
        android:id="@+id/invite_attention_nickname_and_badge"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_toRightOf="@id/invite_attention_avatar_layout"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/invite_attention_nickname"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="8dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:maxWidth="160dp"
            android:textColor="@color/light_grey"
            android:textSize="16sp" />

        <LinearLayout
            android:id="@+id/invite_attention_badge_layout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:layout_marginLeft="4dp"
            android:orientation="horizontal"/>
    </LinearLayout>

    <TextView
        android:id="@+id/invite_attention_phone_number"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/invite_attention_nickname_and_badge"
        android:layout_marginLeft="8dp"
        android:layout_marginRight="60dp"
        android:layout_marginTop="4dp"
        android:layout_toRightOf="@id/invite_attention_avatar_layout"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="#aeaeae"
        android:textSize="14sp" />

    <ImageButton
        android:id="@+id/invite_attention_is_follow"
        android:layout_width="60dp"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:layout_centerVertical="true"
        android:src="@drawable/icon_follow_tomato"
        android:background="@color/transparent" />
</RelativeLayout>