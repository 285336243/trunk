<?xml version="1.0" encoding="utf-8"?>

<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:scrollbars="none">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="32dp"
        android:layout_marginRight="32dp"
        android:layout_gravity="center_horizontal"
        android:orientation="vertical">

        <RelativeLayout
            android:id="@+id/register_avatar"
            android:layout_width="88dp"
            android:layout_height="88dp"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="32dp">

            <ImageView
                android:id="@+id/register_avatar_image"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@drawable/user_header_backgroud" />

            <ImageView
                android:id="@+id/register_takephoto"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:background="@drawable/icon_takephoto_tomato" />
        </RelativeLayout>

        <EditText
            android:id="@+id/register_nickname"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:textSize="16sp"
            android:layout_marginTop="32dp"
            android:drawableLeft="@drawable/icon_username_tomato"
            android:hint="@string/nickname"
            android:textColor="@color/white_grey"
            android:drawablePadding="8dp"
            android:paddingLeft="6dp"
            android:background="@drawable/corner_edit"/>

        <EditText
            android:id="@+id/register_password"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:textSize="16sp"
            android:layout_marginTop="32dp"
            android:drawableLeft="@drawable/icon_password_tomato"
            android:hint="@string/password"
            android:textColor="@color/white_grey"
            android:inputType="textPassword"
            android:drawablePadding="8dp"
            android:paddingLeft="6dp"
            android:background="@drawable/corner_edit"/>

        <Button
            android:id="@+id/register_next"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginTop="32dp"
            android:background="@drawable/corner_button"
            android:text="@string/next"
            android:textColor="@color/white"
            android:textSize="18sp" />
    </LinearLayout>
</ScrollView>