<?xml version="1.0" encoding="utf-8"?>

<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="6dp">

    <LinearLayout
        android:id="@+id/topic_detail_item_root"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="12dp"
        android:layout_marginRight="12dp"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/topic_detail_item_image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:visibility="invisible"
            android:background="@drawable/icon_tagcomment_tomato" />

        <com.socialtv.view.TextViewFixTouchConsume
            android:id="@+id/topic_detail_item_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:lineSpacingExtra="2dp"
            android:textColor="@color/light_grey"
            android:layout_marginLeft="4dp"
            android:textSize="14sp" />
    </LinearLayout>

    <include layout="@layout/list_empty" />
</FrameLayout>